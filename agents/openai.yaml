interface:
  display_name: "Orchestrate with Sol Ultra"
  short_description: "One lead, bounded agents, evidence-first integration"
  default_prompt: "Use $orchestrate-with-sol-ultra to coordinate bounded agents, integrate centrally, and verify the result."

policy:
  allow_implicit_invocation: true
